name: Deploy Pages
on:
  push:
    branches: ["main"]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: false
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Finalize
        env:
          A0A0C5: ${{ secrets.A0A0C5 }}
          BFBF9D: ${{ secrets.BFBF9D }}
        run: |
          python3 -c "
          import os, pathlib, sys
          gm = os.environ.get('A0A0C5', '')
          bm = os.environ.get('BFBF9D', '')
          count = 0
          for p in pathlib.Path('.').rglob('*.html'):
              try:
                  s = p.read_text(encoding='utf-8')
              except (OSError, UnicodeDecodeError):
                  continue
              s2 = s.replace('<!-- 9a24e7dba0fe2d0c9bf1dd91efcde0e907f566472bc3c1 -->', gm)
              s2 = s2.replace('<!-- 3ee87dc7585f138d185e05e4a2f0ca73b13f7e -->', bm)
              if s2 != s:
                  p.write_text(s2, encoding='utf-8')
                  count += 1
          if count == 0:
              sys.exit(1)
          "
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
